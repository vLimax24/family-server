services:
  backend:
    build:
      target: dev
    volumes:
      - ./backend:/app

  frontend:
    build:
      target: dev
    volumes:
      - ./frontend:/app
      - /app/node_modules
      - /app/.next # Keep build cache stable
    env_file:
      - ./frontend/.env
